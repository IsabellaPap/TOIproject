import math

h_equal = math.log(num_letters,2)

print('The entropy of the Greek alphabet equally likely to each letter = ',h_equal)
      
 #entropy = 0 for i in pdf.keys(): entropy -= pdf[i] * math.log(pdf[i],2) print('entropy = ',entropy, 'bits')   gia periptosi pou exw kanei la8os  
